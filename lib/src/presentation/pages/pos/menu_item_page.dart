import 'package:flutter/material.dart';

class MenuItemPage extends StatelessWidget {
  const MenuItemPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('WIP : MENU ITEM PAGE'));
  }
}
